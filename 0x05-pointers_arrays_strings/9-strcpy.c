#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcpy - Copies string values
 * @dest: Destination of copied string
 * @src: Source of coiped string
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src);
}
